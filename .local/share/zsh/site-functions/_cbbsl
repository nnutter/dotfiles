#compdef cbbsl

_cbbsl() {
  local cbbsl_dir='/Users/nnutter/src/github.com/cbbsl'
  local -a subdirs

  if [[ -d "$cbbsl_dir" ]]; then
    subdirs=(${cbbsl_dir}/*(/:t))
    _describe 'repositories' subdirs
  fi
}

_cbbsl "$@"
