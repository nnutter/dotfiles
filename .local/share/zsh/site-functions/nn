nn() {
    if [[ -z "$1" ]]; then
        echo "Usage: nn <repository>"
        return 1
    fi

    local repos_dir="$HOME"'/src/github.com/nnutter'
    local target_dir="$repos_dir/$1"

    if ! [[ -d "$target_dir" ]]; then
        echo "Directory $target_dir does not exist"
        return 1
    fi
    cd "$target_dir"
}
