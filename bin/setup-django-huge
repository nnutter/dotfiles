#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

cd "$(git rev-parse --show-toplevel)"/python

source env.sh
tox -r -e manage -- migrate
tox -e manage loaddata -- gocmat/fixtures/team_auth_users.json
tox -e manage runserver 0.0.0.0:8000
