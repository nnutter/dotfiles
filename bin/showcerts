#!/usr/bin/env bash

set -o errexit -o pipefail

main() {
    local address="$1"
    shift
    echo \
        | openssl s_client -showcerts -connect "$address" 2>/dev/null \
        | perl -ne 'print if /BEGIN CERTIFICATE/../END CERTIFICATE/'
}

main "$@"
