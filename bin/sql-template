#!/usr/bin/env bash

pbpaste \
  | sed \
    -e 's|{{ time_offset_milliseconds }}|1800000|g' \
    -e 's|{{ namespace }}|prod|g' \
    -e 's|{{ project }}|cbbapps-data-prod-ab87c5f1e5|g' \
    -e 's|{{ max_age_in_days }}|10000|g' \
  | pbcopy
