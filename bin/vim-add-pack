#!/usr/bin/env bash

PACKNAME="$1"
GIT_URL="$2"

set -o errexit -o nounset -o pipefail

PACKPATH=$HOME/.vim/pack

gitsick dotfiles submodule add "$GIT_URL" "${PACKPATH}/${PACKNAME}/start/${PACKNAME}"
gitsick dotfiles commit --edit --message "add ${PACKNAME} vim pack" -- "$HOME/.gitmodules" "${PACKPATH}/${PACKNAME}/start/${PACKNAME}"
